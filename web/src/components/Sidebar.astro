---
import { strings } from '../i18n/strings'

interface NavItem {
  key: string
  label: string
  sub: string
  count: number
}

interface Props {
  items: NavItem[]
  total: number
  activeKey?: string
}

const { items, total, activeKey = items[0]?.key } = Astro.props
---

<aside class="sidebar">
  <div class="sidebar-brand">
    <div class="font-display" style="font-size: 28px; font-weight: 400; line-height: 1.2; letter-spacing: -0.01em;">
      {strings.sidebarBrand}
    </div>
    <div class="font-display" style="font-size: 14px; color: var(--text-secondary); font-style: italic; margin-top: 2px;">
      {strings.sidebarSubtitle}
    </div>
    <div style="font-size: 11px; color: var(--text-secondary); margin-top: 8px; letter-spacing: 0.04em;">
      共收錄 <span class="font-display" style="font-size: 14px; color: var(--accent); font-weight: 500;">{total}</span> 條
    </div>
  </div>

  <nav class="sidebar-nav">
    {items.map(({ key, label, sub, count }) => (
      <a
        href={`#${key}`}
        class:list={['sidebar-item', { active: key === activeKey }]}
        data-key={key}
      >
        <div>
          <div class="sidebar-item-label">{label}</div>
          <div class="sidebar-item-sub">{sub}</div>
        </div>
        <span class="sidebar-item-count">{count}</span>
      </a>
    ))}
  </nav>

</aside>

<!-- Mobile horizontal tab bar -->
<nav class="mobile-tabs">
  {items.map(({ key, label, count }) => (
    <a
      href={`#${key}`}
      class:list={['mobile-tab', { active: key === activeKey }]}
      data-key={key}
    >
      {label} <span class="mobile-tab-count">{count}</span>
    </a>
  ))}
</nav>

<style>
  .sidebar {
    background: var(--sidebar-bg);
    border-right: 1px solid var(--divider);
    position: sticky;
    top: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    overflow-y: auto;
  }

  .sidebar-brand {
    padding: 32px 20px 24px;
    border-bottom: 1px solid var(--divider);
  }

  .sidebar-nav {
    flex: 1;
    padding: 12px 0;
  }

  .sidebar-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 20px;
    border-left: 3px solid transparent;
    cursor: pointer;
    transition: background-color 0.15s ease, border-color 0.15s ease;
    text-decoration: none;
    color: inherit;
  }

  .sidebar-item:hover {
    background-color: #F0EDE8;
    color: inherit;
  }

  .sidebar-item.active {
    border-left-color: var(--accent);
    background-color: rgba(180, 83, 9, 0.06);
  }

  .sidebar-item-label {
    font-size: 14px;
    font-weight: 500;
    line-height: 1.4;
  }

  .sidebar-item-sub {
    font-size: 11px;
    color: var(--text-secondary);
    letter-spacing: 0.03em;
    margin-top: 1px;
  }

  .sidebar-item-count {
    font-family: 'Inter', sans-serif;
    font-size: 12px;
    color: var(--text-secondary);
    background: rgba(0,0,0,0.04);
    padding: 1px 8px;
    border-radius: 10px;
    flex-shrink: 0;
  }

  /* Mobile tabs */
  .mobile-tabs {
    display: none;
  }

  @media (max-width: 768px) {
    .sidebar {
      display: none;
    }

    .mobile-tabs {
      display: flex;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      gap: 0;
      padding: 0 12px;
      background: var(--sidebar-bg);
      border-bottom: 1px solid var(--divider);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .mobile-tab {
      flex-shrink: 0;
      padding: 10px 14px;
      font-size: 13px;
      font-weight: 500;
      color: var(--text-secondary);
      text-decoration: none;
      border-bottom: 2px solid transparent;
      white-space: nowrap;
      transition: color 0.15s ease, border-color 0.15s ease;
    }

    .mobile-tab:hover {
      color: var(--text-primary);
    }

    .mobile-tab.active {
      color: var(--text-primary);
      border-bottom-color: var(--accent);
    }

    .mobile-tab-count {
      font-size: 11px;
      color: var(--text-secondary);
      margin-left: 2px;
    }
  }
</style>
